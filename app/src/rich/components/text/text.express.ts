
import { BlockType } from "../../block/block.type";
import { VcExpress } from "../express";
export var VcText: VcExpress = {
    type: BlockType.text,
    propNames: ['text'],
    modePropNames: []
}